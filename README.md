
  # Design CapSort Front-End UI

  This is a code bundle for Design CapSort Front-End UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  